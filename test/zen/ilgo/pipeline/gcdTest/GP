# 20 very big Integers
# each integer is 301030 digits long, generated by the following statement

random(2^10^6)


# these 3 lines of pari-code will create the filepath to the 20 bigIntegers
# and then calculate the GCD for each Pair. 1+2, 3+4, 5+6,......

file="/home/ilgo/Code/workspace/zen.ilgo.tools/test/zen/ilgo/pipeline/gcdTest/BigIntegers/"
bigs=concat(vector(9,x,Str("biggies0",x)),vector(11,x,Str("biggies",x+9)))
forstep(x=1,19,2,a=read(Str(file,bigs[x]));b=read(Str(file,bigs[x+1]));print(gcd(a,b)))

# here is the output

? #
   timer = 1 (on)
? forstep(x=1,19,2,a=read(Str(file,bigs[x]));b=read(Str(file,bigs[x+1]));print(gcd(a,b)))
1
3
4
1
1
3
1
2
1
2
time = 28,300 ms.
------------------------------
there is also a gdc.c file that calculates by using gmp
it compiles with 
gcc gdc.c -lgmp


